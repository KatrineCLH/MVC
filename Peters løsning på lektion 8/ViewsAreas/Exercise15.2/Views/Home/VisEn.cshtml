@using Exercise15_2.Models

@model EnOgAlleViewModel
@{
    ViewBag.Title = "VisEn";
}

<h2>VisEn</h2>

@*@(Model.Fornavn + " " + @Model.Efternavn)*@

@{
    // @Html.RenderPartial("VisEnPerson", Model.ValgtPerson);
    @Html.Action("VisEnPerson", "Home", new { p = Model.ValgtPerson });
}

@section AlleSection
{
    <p> Nedenfor er alle personer, vist i en section</p>

    @foreach (Person p in Model.AllePersoner)
    {
       @Html.Action("VisEnPerson", "Home", new { P = p });
        <br />
    }

}

